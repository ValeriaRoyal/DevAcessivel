import React from 'react';
import styled from 'styled-components';

// Versão simplificada do DesignSystemDemo para deploy
const Container = styled.div`
  padding: 24px;
`;

const DesignSystemDemo = () => {
  return (
    <Container>
      <h1>Design System</h1>
      <p>Esta página está temporariamente simplificada para deploy.</p>
    </Container>
  );
};

export default DesignSystemDemo;
